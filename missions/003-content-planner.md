## MISSION: CONTENT PLANNER — TBD

**Status:** Starter brief. To be expanded next session with Sejn + Cejv onboarding.

**OBJECTIVE:** Stand up a repeatable social content pipeline for The Truss People — multi-human collaboration (Pits + Sejn + Cejv) with Claude Code in the loop. Output: weekly/biweekly social posts (reels + stills) on a planner that doesn't depend on any single person being available.

**AO:**
- `content/posts/` — already scaffolded, contains in-flight post 003 (drone flyover, 9:16, 15-20s, "precision at scale" angle)
- Source clips from operator GDrive (job site footage, drone, factory)
- Output destinations: TBD (Instagram / Meta Business Suite / TikTok / direct send to Victor for client review)

**INTEL:**
- Post 002 already shipped (per WORKLOG history) — established the visual register
- Post 003 queued: drone flyover, 9:16, 15-20s — angle "precision at scale" (extends post 002)
- 19 gallery photos still need real job suburb/builder/type metadata from Victor & Tony — that's the supply-side bottleneck for a sustained content cadence
- Interstate tiles (Tas + NSW) need real photos — same supply-side ask
- Operator preference: short sharp briefs, action lists not essays (per `feedback_short_sharp_briefs.md`)

**TEAM:**
- **Pits (operator)** — strategy, client comms, final approval, tool/stack decisions
- **Sejn** — TBD (confirm role next session: editor / strategist / on-site capture?)
- **Cejv** — TBD (confirm role next session)
- **Claude Code** — pipeline scaffolding, ffmpeg / cut / format ops, copy drafting, planner upkeep

**OPEN QUESTIONS (resolve next session):**
- What does the planner actually look like? Notion DB / Google Sheet / file-based in `content/`?
- Cadence target — weekly? biweekly?
- Who owns each step in the pipeline (capture → cut → copy → preview → approve → post)?
- Who has the Meta/IG account and posts the final content?
- Approval flow with Victor — async via WhatsApp or scheduled review?
- Asset library / source-of-truth folder for raw clips (currently scattered in operator GDrive)?
- Style guide — pin the visual register established in posts 001 + 002 so output stays consistent across editors

**CONSTRAINTS:**
- Must work when only one of three humans is available (no single point of failure)
- Source clip review can't bottleneck on Victor — pre-approve a "batch" of footage so the team has runway
- Brand register is conservative trade-business: precision / scale / craftsmanship — not influencer-casual

**DEFINITION OF DONE (for the mission, not a single post):**
- Documented pipeline (CAPTURE → EDIT → COPY → PREVIEW → APPROVE → POST) with named owners per step
- Planner artifact (location TBD) showing next 4 posts queued with drafts in motion
- Post 003 shipped as the first post through the new pipeline
- Sejn + Cejv onboarded to Claude Code with the pieces of CLAUDE.md they need

**NEXT MOVE:**
Operator to expand this brief from Claude.ai during onboarding session with Sejn + Cejv. Then re-enter via Claude Code for execution.

**Related:**
- Plan file: TBD (will be created in planning session)
- WORKLOG mention: STATUS line will update once content workstream is the active mission again
- Existing in-flight: `content/posts/003-precision-at-scale/`
